{
  "name": "lights-out",
  "version": "1.0.0",
  "description": "An implementation of the Lights Out game in pure JS",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf bin",
    "start": "yarn clean && yarn build && node src/server.js"
  },
  "author": "Matias Piercamilli",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.7",
    "@types/node": "^14.0.23",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "express": "^4.17.3"
  }
}
