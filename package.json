{
  "name": "lights-out",
  "version": "1.0.0",
  "description": "An implementation of the Lights Out game in pure JS",
  "scripts": {
    "clean": "rimraf src/bin",
    "start": "yarn clean && tsc && node src/server.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.0.23",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
